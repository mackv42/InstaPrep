@using InstaPrep.Data

@page "/recipedetail/{RecipeID}"

<PageTitle>Recipe Detail</PageTitle>

<MudText Typo="Typo.h4" GutterBottom="true">Recipe Detail</MudText>

@code{
        [Parameter]
        public string? RecipeID { get; set;}
        protected override void OnParametersSet()
        {
            base.OnParametersSet();
            var test = RecipeID;
        }
}